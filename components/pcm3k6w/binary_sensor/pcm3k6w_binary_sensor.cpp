#include "pcm3k6w_binary_sensor.h"

namespace esphome::pcm3k6w {

// PCM3K6WBinarySensor currently adds no behaviour beyond binary_sensor::BinarySensor;
// state is pushed to it directly by PCM3K6WComponent::publish_binary_sensor_().

}  // namespace esphome::pcm3k6w
