# sensor_combined is a platform-only component: it does not define a
# new top-level YAML key. All the config schema and code generation for
# the "sensor_combined" platform of the "sensor" domain live in sensor.py.
CODEOWNERS = ["@SeByDocKy"]
